name: Fixed Workflow
on:
  push:
    branches: [ main ]
jobs:
  fixed_job:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Fixed Step
        run: |
          # This workflow has restricted token permissions
          echo "Running with restricted permissions"
