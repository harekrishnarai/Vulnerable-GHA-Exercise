name: Fixed Workflow
on:
  pull_request_target:
    types: [opened]
jobs:
  fixed_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.base.sha }}
      - name: Fixed Step
        run: |
          # This script is from the base branch, so it is safe
          ./run.sh
