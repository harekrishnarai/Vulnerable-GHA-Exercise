name: Fixed Workflow
on:
  push:
    branches: [ main ]
jobs:
  fixed_job:
    runs-on: ubuntu-latest
    steps:
      - name: Fixed Step
        run: |
          # This workflow now runs on a GitHub-hosted runner.
          echo "Running on a GitHub-hosted runner"
