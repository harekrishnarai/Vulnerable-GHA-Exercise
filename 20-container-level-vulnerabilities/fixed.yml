name: Fixed Workflow
on:
  push:
    branches: [ main ]
jobs:
  fixed_job:
    runs-on: ubuntu-latest
    container:
      image: node:18
    steps:
      - name: Fixed Step
        run: |
          # This workflow now uses a more recent version of node.
          echo "Running on node:18"
