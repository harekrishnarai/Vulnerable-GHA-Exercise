name: Fixed Workflow
on:
  push:
    branches: [ main ]
jobs:
  fixed_job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: |
          # This workflow uses a scoped package name to prevent dependency confusion.
          npm install @my-org/my-package
